# 데빌 커넥션 한글패치 비공개 배포 시스템 계획서

## 1. 문서 목적

이 문서는 현재 Python/PyQt6 기반 한글패치 설치기를 Rust 기반 설치기로 전환하고, 공개 VPS를 중계 계층으로 사용해 보유 중인 NAS에서 승인된 사용자에게만 개인화된 번역 패키지를 제공하기 위한 구현 계획을 정의한다.

목표는 숙련된 역공학자를 상대로 완전한 복제를 방지하는 것이 아니다. 다음과 같은 일반적인 무단 재배포를 억제하고, 유출된 번역본의 배포 경로를 식별하는 것이 목표다.

- 미실행 설치 파일의 단순 공유
- 초대 코드의 무단 재사용
- 다운로드한 번역 패키지의 다른 컴퓨터 사용
- 설치된 `app.asar`의 재배포

권장 방식은 **번역 데이터를 포함하지 않은 공용 Rust 설치기**, **NAS의 수동 승인형 기기 등록**, **기기별 암호화 패키지**, **개인별 배포 지문**을 결합하는 것이다.

## 2. 현재 프로젝트 분석

### 2.1 현재 설치 방식

현재 `main.py`는 다음 순서로 동작한다.

1. 운영체제별 게임 경로에서 `app.asar`를 찾는다.
2. 최초 설치 시 `app.asar.backup`을 만든다.
3. 기존 임시 `app` 폴더를 제거한다.
4. 원본 `app.asar`를 임시 폴더로 해제한다.
5. 번역 데이터를 덮어쓴다.
6. 기존 `app.asar`를 제거하고 새 아카이브를 만든다.
7. 임시 폴더를 제거한다.

현재 배포 대상은 다음과 같다.

- Windows x64
- macOS arm64
- Linux x86_64

현재 설치기에 포함되는 주요 데이터 경로는 다음과 같다.

- `data/scenario`
- `data/others`
- `data/system`
- `data/fgimage`
- `data/image`
- `data/video`
- `data/bgimage`
- `tyrano`

현재 번역 데이터는 약 139MB이며, `.ks` 파일 약 382개, PNG 파일 약 333개, MP4 파일 9개 등을 포함한다.

### 2.2 현재 방식의 배포상 한계

- PyInstaller 실행 파일 안에서 번역 데이터를 추출할 수 있다.
- 설치 후 번역 데이터는 게임의 `app.asar`에 들어가므로 설치기 인증만으로는 재배포를 막을 수 없다.
- ASAR은 암호화 형식이 아니므로 Node.js 도구로 해제할 수 있다.
- 기존 코드는 새 `app.asar` 생성 전에 원본을 제거하므로, 재압축 실패 시 게임 파일이 불완전해질 수 있다.
- 기기 등록, 라이선스 서명, 철회, 기기 변경, 업데이트 권한 관리 기능이 없다.

Rust 전환 시 기존 설치 동작을 보존하되, 새 아카이브를 별도 경로에 완성하고 검증한 뒤 교체하는 트랜잭션 방식으로 개선한다.

## 3. 목표와 비목표

### 3.1 목표

- 공용 설치기에는 번역 원본을 포함하지 않는다.
- 관리자가 승인한 사용자와 기기만 번역 패키지를 받을 수 있다.
- 한 초대 코드는 승인된 기기 하나에만 귀속한다.
- 다운로드한 암호화 패키지는 다른 기기에서 열리지 않는다.
- 설치 결과물에는 개인정보가 아닌 무작위 배포 ID를 남긴다.
- NAS 장애가 발생해도 이미 설치한 사용자는 게임을 실행할 수 있다.
- 사용자는 NAS와 가정 회선의 공인 IP를 알 수 없고 VPS 주소만 볼 수 있다.
- 기기 교체와 라이선스 철회를 관리자가 처리할 수 있다.
- 원본 게임 파일을 안전하게 백업하고 설치 실패 시 복구할 수 있다.

### 3.2 비목표

- 숙련된 분석가의 메모리 덤프나 바이너리 패치를 완전히 차단하지 않는다.
- 설치된 번역 텍스트가 어떤 방법으로도 추출되지 않음을 보장하지 않는다.
- 철회 후 이미 설치된 데이터를 원격 삭제하지 않는다.
- 게임 실행 때마다 온라인 접속을 강제하지 않는다.
- 화면 캡처, 수동 전사, 영상 녹화를 막지 않는다.

## 4. 착수 전 필수 결정: 저작권과 라이선스

현재 `README.md`는 번역 텍스트와 패치 스크립트를 CC BY-NC-SA 4.0으로 배포한다고 명시한다. 이 라이선스는 조건을 지킨 비상업적 복사와 재배포를 허용하고, 허용된 행위를 제한하는 추가적인 기술 조치를 두지 못하도록 규정한다.

기존 공개본에 부여된 CC 라이선스는 공개 중단만으로 소급 철회되지 않는다. 이후 버전을 다른 조건으로 배포하려면 번역, 이미지, 영상, 코드 등 각 구성요소의 권리관계와 공동 기여자의 동의를 먼저 확인해야 한다.

구현 착수 전 다음 사항을 문서로 확정한다.

- 기존 공개본과 향후 비공개 버전의 범위 구분
- 번역팀 구성원별 기여분과 재라이선스 동의 여부
- 이미지 및 영상 번역본의 사용·재배포 조건
- 포함 서체별 재배포 및 암호화 패키징 허용 여부
- 설치기 코드와 번역 데이터의 라이선스 분리 여부
- 사용자에게 고지할 기기 등록, 로그, 배포 지문 정책

참고 자료:

- [CC BY-NC-SA 4.0 공식 설명](https://creativecommons.org/licenses/by-nc-sa/4.0/)
- [Creative Commons DRM FAQ](https://creativecommons.org/faq/#can-i-use-effective-technological-measures-such-as-drm-when-i-share-cc-licensed-material)

이 항목이 정리되지 않은 상태에서는 기기 귀속형 배포를 실제 운영에 투입하지 않는다.

## 5. 권장 시스템 구조

```text
┌──────────────────── 사용자 PC ────────────────────┐
│ 공용 Rust 설치기                                  │
│  ├─ 초대 코드 입력                                │
│  ├─ 기기 키 생성 및 보관                          │
│  ├─ 등록 요청                                     │
│  ├─ 서버·패키지 서명 검증                         │
│  ├─ 개인화 패키지 복호화                          │
│  └─ app.asar 백업·패치·복구                       │
└────────────────────────┬──────────────────────────┘
                         │ HTTPS
                         ▼
┌──────────────────── 공개 VPS ─────────────────────┐
│ HTTPS 종료 및 리버스 프록시                        │
│ 연결·요청 속도 제한                                │
│ 선택적 암호문 패키지 캐시                          │
└────────────────────────┬──────────────────────────┘
                         │ WireGuard 사설 터널
                         ▼
┌────────────────────── NAS ────────────────────────┐
│ patch-api                                          │
│  ├─ 등록 요청과 관리자 승인                        │
│  ├─ 기기 인증                                      │
│  ├─ 단기 다운로드 토큰 발급                        │
│  └─ 감사 로그                                      │
│                                                    │
│ SQLite                                             │
│ 패키지 빌더 및 개인화 패키지 저장소               │
│ 로컬 전용 관리자 CLI                              │
└────────────────────────────────────────────────────┘
```

공개 DNS는 VPS 주소만 가리킨다. VPS는 HTTPS 요청을 받고 WireGuard 사설 주소를 통해 NAS의 `patch-api`로 전달한다. NAS는 NAT 안쪽에서 VPS로 WireGuard 연결을 먼저 만들고 유지하므로 공유기의 인바운드 포트, NAS DDNS, NAS 공인 주소 공개가 필요 없다. NAT 뒤의 피어가 연결 상태를 유지해야 할 때는 WireGuard의 `PersistentKeepalive`를 사용한다.

VPS는 가능한 한 상태 없는 중계기로 유지한다. 서버 서명키, 활성화 데이터베이스, 평문 번역 원본, 패키지 마스터 키는 NAS에만 둔다. 초기 버전에서는 패키지를 NAS에서 VPS를 거쳐 사용자에게 스트리밍하고, 트래픽이나 NAS 가용성이 문제가 될 때만 기기별 암호문 패키지를 VPS에 캐시한다.

이 구조는 일반 사용자와 인터넷 스캔에서 가정 회선의 공인 IP를 숨긴다. WireGuard 연결의 상대방인 VPS 사업자는 NAS의 접속 IP를 볼 수 있으므로, VPS 사업자에게까지 원본 IP를 숨기는 구조는 목표에 포함하지 않는다.

참고 자료:

- [WireGuard 공식 Quick Start](https://www.wireguard.com/quickstart/)
- [Caddy reverse_proxy 공식 문서](https://caddyserver.com/docs/caddyfile/directives/reverse_proxy)

## 6. 사용자 흐름

### 6.1 신규 사용자 등록

1. 관리자가 수령인 별칭과 무작위 초대 코드를 생성한다.
2. 초대 코드는 기존에 신원을 확인한 연락 채널로 전달한다.
3. 사용자는 공용 Rust 설치기를 실행하고 초대 코드를 입력한다.
4. 설치기는 서명용 기기 키와 암호화용 기기 키를 생성한다.
5. 설치기는 기기 공개키, 솔트 처리된 기기 식별 해시, 요청 임의값을 NAS에 보낸다.
6. NAS는 요청을 `pending` 상태로 저장하고 짧은 요청 번호를 반환한다.
7. 사용자는 기존 연락 채널로 요청 번호를 관리자에게 전달한다.
8. 관리자는 NAS 로컬 CLI에서 수령인과 요청 번호를 확인하고 승인한다.
9. NAS는 기기 공개키에 귀속된 서명 라이선스를 발급한다.
10. 설치기는 승인 상태를 확인하고 개인화 패키지를 다운로드한다.

수동 승인 단계는 미실행 설치기나 초대 코드를 먼저 입수한 제3자가 자동으로 기기를 선점하는 문제를 막는다.

### 6.2 설치

1. 설치기는 서버가 보낸 임의값에 기기 개인키로 서명한다.
2. 서버는 라이선스 상태와 기기 서명을 확인한다.
3. 서버는 수명이 짧고 한 번만 사용할 수 있는 다운로드 토큰을 발급한다.
4. 설치기는 해당 기기용으로 암호화된 개인화 패키지를 받는다.
5. 설치기는 서버 서명, 패키지 버전, 파일 목록과 해시를 검증한다.
6. 게임 버전과 `app.asar` 구조가 지원 대상인지 확인한다.
7. 충분한 디스크 공간이 있는지 확인한다.
8. 임시 작업 디렉터리에서 새 `app.asar`를 생성한다.
9. 새 아카이브를 다시 열어 필수 파일과 해시를 검증한다.
10. 기존 파일을 백업하고 새 파일을 원자적으로 교체한다.
11. 실패하면 백업을 복원하고 명시적인 오류를 표시한다.
12. 성공하면 임시 평문 파일을 정리한다.

### 6.3 업데이트

- 기존 기기 라이선스와 기기 서명으로 최신 버전을 확인한다.
- 같은 배포 ID를 유지하면서 버전별 개인화 패키지를 생성한다.
- 업데이트에는 관리자 재승인을 요구하지 않는다.
- 철회된 라이선스에는 새 패키지를 제공하지 않는다.
- 서버 장애 시 기존 설치본은 계속 실행되고 업데이트만 보류된다.

### 6.4 기기 변경과 철회

- 사용자가 기기를 변경하면 관리자가 기존 등록을 해제한다.
- 재등록 횟수와 사유를 감사 로그에 기록한다.
- 철회는 신규 설치와 업데이트를 막지만 이미 설치된 파일을 원격 삭제하지 않는다.
- 분실이나 오탐 처리를 위해 `active`, `pending`, `revoked`, `reset` 상태를 구분한다.

## 7. 구성요소별 설계

### 7.1 공용 Rust 설치기

필수 기능:

- Windows, macOS, Linux 게임 경로 자동 감지 및 수동 선택
- 초대 코드 입력과 등록 상태 표시
- 기기 키 생성과 OS 보안 저장소 보관
- 기기 식별 해시 생성
- 서버 인증서와 패키지 서명 검증
- 재전송 공격을 막는 challenge-response 인증
- 단기 다운로드 토큰 사용
- 다운로드 재개와 명확한 오류 처리
- ASAR 해제 및 재생성
- 설치 전 버전·공간·권한 검사
- 트랜잭션 설치와 자동 복구
- 로그에서 초대 코드, 키, 전체 기기 식별값 제거

Rust는 분석 난도를 높이는 수단일 뿐 보안 경계로 간주하지 않는다. 서버 비밀키와 전체 번역 원본은 설치기에 넣지 않는다.

### 7.2 NAS 활성화 API

필수 기능:

- 초대 코드 검증과 사용 횟수 제한
- 등록 요청 생성과 상태 조회
- 관리자 승인 결과 반영
- 서명된 기기 라이선스 발급
- challenge-response 검증
- 패치 버전과 지원 게임 버전 제공
- 짧은 수명의 일회용 다운로드 토큰 발급
- 속도 제한과 반복 실패 차단
- 라이선스 철회와 기기 재등록
- 최소한의 감사 로그

초기 버전은 별도 웹 관리자 화면을 만들지 않는다. 관리자 기능은 NAS 로컬 셸 또는 VPN에서만 접근 가능한 CLI로 제공해 외부 공격 표면을 줄인다.

### 7.3 개인화 패키지 빌더

빌더는 기준 번역 데이터에서 사용자별 패키지를 만든다.

- 기준 파일 목록과 SHA-256 매니페스트 생성
- 무작위 `distributionId` 삽입
- 검수된 문장 변형표에서 결정론적으로 변형 조합 선택
- 필요한 경우 이미지 또는 데이터 메타데이터에 배포 ID 삽입
- 개인화 결과와 변형 선택을 관리자용 기록으로 저장
- 완성 패키지에 서버 서명 적용
- 기기 공개키용으로 패키지 키를 봉인
- 패키지 생성 중 평문 임시 파일 안전 정리

번역 품질 저하를 막기 위해 문장 변형은 별도 검수 파일에 등록된 표현만 사용한다. 사용자 이름, 이메일, 기기 시리얼 같은 개인정보는 콘텐츠에 넣지 않는다.

첫 구현은 버전·사용자별 전체 패키지를 한 번 생성하고 NAS에 캐시한다. 사용자 수나 보존 버전이 많아져 저장량이 문제가 되면 기준 패키지와 개인별 오버레이를 분리하거나 요청 시 스트리밍 생성하는 방식으로 개선한다.

### 7.4 라이선스와 패키지 형식

라이선스에 포함할 항목:

- 라이선스 ID
- 무작위 배포 ID
- 기기 서명 공개키 해시
- 기기 암호화 공개키 해시
- 허용된 패치 채널
- 발급 시각
- 선택적 만료 시각
- 형식 버전
- 서버 서명

패키지에 포함할 항목:

- 패키지 형식 버전
- 패치 버전
- 지원 게임 버전 또는 원본 파일 조건
- 파일 경로, 크기, 해시 목록
- 배포 ID
- 암호화된 번역 데이터
- 서버 서명

서버 서명에는 Ed25519 등 검증된 서명 방식을 사용한다. 데이터 암호화는 검증된 라이브러리의 공개키 봉인과 AEAD를 사용하고 자체 암호 프로토콜을 설계하지 않는다. 정확한 라이브러리와 직렬화 형식은 기술 검증 단계에서 상호운용성, 유지보수성, 감사를 기준으로 확정한다.

## 8. 데이터 모델 초안

### `recipients`

- `id`
- `alias`
- `distribution_id`
- `status`
- `created_at`

### `invites`

- `id`
- `recipient_id`
- `token_hash`
- `expires_at`
- `used_at`
- `status`

초대 코드 원문은 저장하지 않고 충분히 긴 무작위 값을 발급한 뒤 해시만 저장한다.

### `devices`

- `id`
- `recipient_id`
- `signing_public_key`
- `encryption_public_key`
- `fingerprint_hash`
- `status`
- `approved_at`
- `revoked_at`

### `patch_versions`

- `id`
- `version`
- `channel`
- `manifest_hash`
- `created_at`
- `status`

### `packages`

- `id`
- `recipient_id`
- `device_id`
- `patch_version_id`
- `file_path`
- `file_hash`
- `created_at`

### `audit_events`

- `id`
- `recipient_id`
- `device_id`
- `event_type`
- `result`
- `created_at`

감사 로그에는 초대 코드, 개인키, 원본 기기 식별값을 저장하지 않는다. IP 주소가 꼭 필요하지 않으면 수집하지 않고, 필요하다면 보존 기간과 마스킹 정책을 먼저 정한다.

## 9. 외부 API 초안

정확한 요청 형식은 구현 전 별도 API 명세로 확정한다.

- `GET /v1/health`: 최소 상태 확인
- `POST /v1/enrollments`: 등록 요청 생성
- `GET /v1/enrollments/{id}`: 승인 상태 조회
- `POST /v1/auth/challenge`: 일회용 인증 challenge 발급
- `POST /v1/auth/verify`: 기기 서명 검증 및 세션 토큰 발급
- `GET /v1/patches/latest`: 사용 가능한 최신 패치 확인
- `POST /v1/downloads`: 일회용 다운로드 토큰 발급
- `GET /v1/packages/{packageId}`: `Authorization` 헤더의 일회용 토큰으로 암호화 패키지 다운로드

관리자 승인, 철회, 기기 초기화 기능은 외부 API에 넣지 않고 로컬 CLI로 시작한다.

다운로드 토큰은 URL이나 쿼리 문자열에 넣지 않는다. URL은 VPS, 애플리케이션, 접근 로그에 남기 쉬우므로 인증정보는 헤더로 전달하고 로그에서 제거한다.

## 10. VPS·NAS 배포와 운영

### 10.1 네트워크 경로

```text
patch.example.com
        │ DNS A/AAAA는 VPS만 지정
        ▼
VPS 공개 주소:443
        │ HTTPS 종료, 요청 제한, 역방향 프록시
        ▼
VPS WireGuard 주소
        │ 암호화된 사설 터널
        ▼
NAS WireGuard 주소:patch-api 포트
```

- 사용자는 VPS의 443 포트에만 연결한다.
- NAS는 VPS의 WireGuard 포트로 먼저 연결하고 NAT 매핑을 유지한다.
- 공유기에는 NAS로 향하는 포트 포워딩과 UPnP 규칙을 만들지 않는다.
- NAS의 `patch-api`는 WireGuard 인터페이스 또는 해당 사설 주소에만 바인딩한다.
- NAS 방화벽은 VPS의 WireGuard 피어 주소에서 오는 API 요청만 허용한다.
- DNS에는 가정 회선 IP, NAS DDNS 주소, NAS의 IPv6 주소를 등록하지 않는다.
- 응답 헤더, 오류 화면, 리디렉션, 다운로드 URL에는 NAS 주소를 포함하지 않는다.
- 설치기 설정과 바이너리에도 NAS IP나 DDNS 이름을 넣지 않는다.

초기 구현은 VPS에서 TLS를 종료하고 Caddy 또는 동급 리버스 프록시로 NAS의 WireGuard 사설 주소에 전달한다. 서버 서명과 기기별 패키지 암호화는 애플리케이션 계층에서 별도로 검증하므로 VPS가 응답을 변경해도 설치기는 위조된 라이선스나 패키지를 받아들이지 않는다.

VPS 운영자에게도 등록 코드와 API 본문을 노출하지 않아야 하는 요구가 추가되면 TLS를 NAS에서 종료하고 VPS는 TCP 계층만 중계하는 구조로 전환한다. 초기 위협 모델에서는 운영 복잡도가 낮고 속도 제한과 오류 처리가 쉬운 HTTPS 리버스 프록시 방식을 사용한다.

### 10.2 VPS 배포 원칙

- Linux VPS에는 WireGuard와 리버스 프록시만 우선 설치한다.
- 공개 포트는 HTTPS와 WireGuard에 필요한 포트로 제한한다.
- SSH는 키 인증만 사용하고 가능한 경우 관리 IP 또는 별도 관리 VPN으로 제한한다.
- VPS에는 서버 서명키, 기기 개인키, 평문 번역 원본, SQLite 원본을 저장하지 않는다.
- 리버스 프록시 접근 로그에서 `Authorization`, 쿠키, 요청 본문을 기록하지 않는다.
- 애플리케이션 오류 응답을 그대로 노출해 내부 NAS 주소가 새지 않도록 한다.
- 연결 수와 등록·인증 요청 속도를 제한한다.
- 다운로드 스트리밍에 적절한 본문 크기, 읽기 시간, 유휴 시간 제한을 설정한다.

패키지 다운로드는 다음 두 단계로 운영한다.

1. MVP에서는 NAS의 기기별 암호문 패키지를 VPS가 그대로 스트리밍한다.
2. 사용량이 늘면 서명된 암호문만 VPS에 캐시하고, NAS가 발급한 일회용 토큰을 검증한 뒤 제공한다.

VPS 캐시가 노출돼도 패키지 평문이나 서버 서명키가 드러나지 않아야 한다. 캐시 도입은 MVP 이후 별도 위협 검토와 무효화 정책을 거쳐 진행한다.

### 10.3 NAS 배포 원칙

- NAS가 컨테이너를 지원하면 API를 비특권 컨테이너로 실행한다.
- WireGuard는 NAS 운영체제 지원 방식 또는 권한을 제한한 전용 컨테이너로 실행한다.
- 컨테이너에 NAS 전체 파일 시스템을 마운트하지 않는다.
- 번역 기준 데이터는 빌더만 읽을 수 있게 한다.
- 사용자 API는 데이터베이스와 패키지 저장소에 필요한 최소 권한만 가진다.
- NAS 관리 UI, SSH, 관리자 CLI는 인터넷에 공개하지 않는다.
- API 로그와 NAS 시스템 로그를 분리한다.
- 운영·개발 환경의 키와 데이터베이스를 분리한다.

### 10.4 백업

다음 항목을 암호화해 NAS 외부에도 백업한다.

- 서버 서명키와 복구 절차
- SQLite 데이터베이스
- 개인화 변형 설정
- 패치 버전 매니페스트
- 배포 ID와 수령인 대응표
- 컨테이너 설정과 환경 구성

서명키를 잃으면 기존 설치기가 새 라이선스와 패키지를 신뢰하지 못하므로 키 백업과 복구 훈련을 필수로 한다. 백업에서 복원한 시험 환경으로 정기적으로 복구 가능 여부를 확인한다.

### 10.5 장애 정책

- NAS 중단: 신규 등록, 신규 설치, 업데이트만 불가
- VPS 중단: 외부 API와 다운로드가 중단되지만 NAS 관리와 기존 설치는 영향 없음
- 기존 설치: 영향 없음
- WireGuard 터널 장애: NAS와 동일하게 온라인 기능만 일시 중단
- 데이터베이스 손상: 읽기 전용 모드로 전환하고 백업 복구
- 서명키 노출: 새 키로 교체하고 설치기 업데이트가 필요하므로 별도 사고 대응 절차 마련

## 11. 보안 및 개인정보 원칙

- 전체 기기 시리얼 대신 라이선스별 솔트를 사용한 해시를 저장한다.
- 기기 키를 주 식별자로 사용하고 HWID는 복제 탐지용 보조 신호로 사용한다.
- 기기 정보 수집 항목, 목적, 보존 기간, 기기 초기화 절차를 사용자에게 고지한다.
- 배포 지문에는 무작위 ID만 넣고 실명이나 연락처를 넣지 않는다.
- 서버 비밀키와 기준 번역 원본은 클라이언트에 포함하지 않는다.
- 모든 네트워크 요청은 HTTPS를 사용한다.
- 등록, 인증, 다운로드 요청에 속도 제한을 둔다.
- 다운로드 토큰은 짧은 시간만 유효하며 한 번 사용하면 폐기한다.
- 서버가 보낸 임의값에 기기가 서명하도록 해 캡처한 요청의 재사용을 막는다.
- 로그와 오류 메시지에 인증정보가 포함되지 않는지 테스트한다.
- 외부에 공개된 API와 사용 라이브러리를 정기적으로 업데이트하되, 업데이트 전 호환성 테스트를 수행한다.

## 12. 구현 단계

### 0단계: 권리와 운영 정책 확정

산출물:

- 기존·향후 배포본의 라이선스 결정서
- 기여자 동의 기록
- 개인정보 및 배포 지문 고지문
- 기기 변경·철회·지원 정책
- 지원 운영체제 우선순위

완료 조건:

- 기기 귀속형 배포를 적용할 데이터 범위가 명확하다.
- 모든 필요한 권리자의 동의 여부가 확인됐다.

### 1단계: 기술 검증용 프로토타입

작업:

- NAS CPU, 운영체제, 컨테이너 지원 여부 확인
- VPS 운영체제, 전송량, 방화벽, WireGuard 지원 여부 확인
- VPS와 NAS 사이의 WireGuard 연결 및 NAT 유지 검증
- Rust에서 ASAR 해제·재생성 호환성 검증
- 원본 게임이 새 ASAR을 정상 실행하는지 확인
- Rust 클라이언트와 시험 API 사이의 등록·서명 검증 구현
- Windows 한 플랫폼에서 최소 설치 흐름 검증

완료 조건:

- Rust로 생성한 `app.asar`로 게임이 정상 실행된다.
- 승인되지 않은 클라이언트는 시험 패키지를 받지 못한다.
- 다른 기기 키로는 시험 패키지를 열 수 없다.

### 2단계: 패키지 빌더

작업:

- 기준 파일 매니페스트 생성
- 개인화 규칙과 배포 ID 삽입
- 패키지 서명·암호화
- 결과 재현과 감사 기록
- 번역 파일과 에셋 검증

완료 조건:

- 같은 사용자와 버전의 빌드 결과를 추적할 수 있다.
- 서로 다른 사용자 패키지에서 배포 지문이 다르다.
- 파일 변조 시 설치기가 거부할 수 있는 매니페스트가 생성된다.

### 3단계: NAS 활성화 서버

작업:

- SQLite 스키마와 마이그레이션
- 등록·상태 조회·인증·다운로드 API
- 관리자 CLI
- 초대 코드와 기기 초기화
- 감사 로그와 속도 제한
- 컨테이너 배포

완료 조건:

- 한 초대 코드로 두 기기를 승인할 수 없다.
- 승인 대기 요청은 패키지를 받을 수 없다.
- 철회된 라이선스는 새 패키지를 받을 수 없다.
- 관리자 기능은 외부 네트워크에서 접근할 수 없다.

### 4단계: Rust 설치기 본구현

작업:

- 최소 GUI와 설치 로그
- OS별 게임 경로 탐색
- 기기 키 보관
- 등록과 다운로드 흐름
- 트랜잭션 ASAR 교체
- 실패 시 자동 복구
- 임시 데이터 정리

완료 조건:

- 공용 설치기 안에 기준 번역 데이터가 없다.
- 지원되는 게임 버전에서 정상 설치된다.
- 설치 실패 후 원본 게임이 실행된다.
- 서버가 중단돼도 이미 설치한 게임은 실행된다.

### 5단계: VPS·NAS 운영 환경 구축

작업:

- VPS에 리버스 프록시와 WireGuard 배치
- NAS에 API와 WireGuard 피어 배치
- 전용 도메인의 DNS를 VPS에만 연결하고 HTTPS 설정
- VPS·NAS 방화벽과 NAS 볼륨 권한 설정
- 포트 포워딩, UPnP, NAS DDNS 비활성화 확인
- 오류·리디렉션·응답 헤더의 원본 주소 유출 검사
- 데이터베이스·서명키 백업
- 상태 확인과 장애 알림
- 복원 훈련

완료 조건:

- 외부에서는 사용자 API만 접근할 수 있다.
- 외부 사용자에게는 VPS 주소만 보이고 NAS 공인 주소가 노출되지 않는다.
- NAS 관리 화면과 관리자 기능은 공개되지 않는다.
- 백업에서 시험 환경을 복원할 수 있다.

### 6단계: 교차 플랫폼 검증

작업:

- Windows x64 빌드 및 설치 시험
- macOS arm64 빌드 및 설치 시험
- Linux x86_64 빌드 및 설치 시험
- OS별 키 저장소와 권한 처리
- 코드 서명 및 배포 경고 검토
- 게임 업데이트 후 호환성 오류 처리

완료 조건:

- 각 지원 플랫폼의 실제 게임 설치본에서 패치와 복구가 성공한다.
- 잘못된 경로, 권한 부족, 디스크 부족, 손상된 다운로드를 명확히 보고한다.

### 7단계: 제한된 파일럿

작업:

- 내부 사용자 2~5명에게 서로 다른 패키지 제공
- 신규 등록, 업데이트, 기기 변경, 철회 절차 실행
- 유출 식별용 시험 파일로 배포 ID 판독 검증
- 설치 오류와 지원 부담 기록

완료 조건:

- 관리자 문서만으로 승인과 초기화를 처리할 수 있다.
- 파일럿 사용자가 별도 개발자 지원 없이 설치할 수 있다.
- 수집한 로그에 비밀값이나 원본 기기 식별정보가 없다.

### 8단계: 정식 운영

작업:

- 대상 사용자별 초대 코드 발급
- 패치 버전 게시와 변경 내역 관리
- 백업 및 상태 점검 주기 운영
- 게임 업데이트 발생 시 호환성 검사 후 새 패치 게시
- 오탐, 분실, 기기 변경 문의 처리

## 13. 검증 시나리오

### 접근 제어

- 유효하지 않은 초대 코드가 거부된다.
- 만료된 초대 코드가 거부된다.
- 승인 전에는 다운로드할 수 없다.
- 사용된 초대 코드로 두 번째 기기를 등록할 수 없다.
- 철회된 기기는 업데이트할 수 없다.

### 기기 귀속

- 암호화 패키지를 다른 컴퓨터로 복사하면 복호화되지 않는다.
- 라이선스 파일만 복사해도 기기 인증에 실패한다.
- 요청과 응답을 캡처해 재전송하면 거부된다.
- 관리자가 초기화한 뒤에만 새 기기를 등록할 수 있다.

### 패키지 무결성

- 패키지 한 바이트를 변경하면 서명 또는 해시 검증이 실패한다.
- 매니페스트에 없는 파일은 설치하지 않는다.
- 경로 탐색 문자열이 포함된 패키지를 거부한다.
- 지원하지 않는 게임 버전에는 설치하지 않는다.

### 설치 안전성

- 디스크 부족 상황에서 원본이 유지된다.
- 재압축 실패 시 백업이 복원된다.
- 설치 도중 강제 종료 후 재실행하면 복구 절차가 동작한다.
- 기존 백업을 임의로 덮어쓰지 않는다.
- 임시 폴더에 평문 데이터가 계속 남지 않는다.

### 배포 지문

- 각 수령인의 결과물에서 올바른 배포 ID를 판독할 수 있다.
- 눈에 보이는 ID 하나를 제거해도 다른 지점으로 식별할 수 있다.
- 문장 변형이 게임 흐름, 태그, 변수, 줄바꿈을 손상하지 않는다.

### 운영 장애

- NAS가 꺼진 상태에서도 이미 패치한 게임은 실행된다.
- 터널 장애 후 자동으로 다시 연결된다.
- 데이터베이스 백업을 복원해 기존 사용자 상태를 조회할 수 있다.

### 원본 주소 비공개

- 공개 DNS의 A·AAAA 레코드가 VPS 주소만 가리킨다.
- 정상 응답, 오류 응답, 리디렉션, TLS 인증서에 NAS 주소나 DDNS 이름이 없다.
- 설치기와 배포 설정에서 NAS 공인 IP 및 DDNS 문자열이 발견되지 않는다.
- WireGuard를 중단하면 VPS가 일반 오류만 반환하고 원본 주소를 노출하지 않는다.
- 가정용 공유기에 패치 서비스를 위한 포트 포워딩과 UPnP 매핑이 없다.

## 14. 예상 위험과 대응

### 설치된 `app.asar` 공유

- 위험: 설치 후 데이터는 사용자가 통제한다.
- 대응: 다중 지점 배포 지문으로 출처를 식별한다.
- 잔여 위험: 숙련된 사용자는 여러 배포본을 비교해 지문을 제거할 수 있다.

### 기기 키 복사

- 위험: 소프트웨어 키 저장소는 관리자 권한 사용자에게 완전한 보호를 제공하지 못할 수 있다.
- 대응: OS 보안 저장소를 사용하고 기기 식별 해시를 보조 검증한다.
- 향후 선택: 필요하면 TPM 또는 Secure Enclave 기반 키를 플랫폼별로 도입한다.

### NAS 침해

- 위험: 활성화 상태, 패키지, 온라인 서명키가 노출될 수 있다.
- 대응: 최소 권한 컨테이너, 인바운드 포트 최소화, 관리자 기능 비공개, 외부 백업, 정기 업데이트를 적용한다.

### VPS 침해

- 위험: 공격자가 API 트래픽을 관찰·차단하거나 NAS API로 악성 요청을 보낼 수 있다.
- 대응: VPS에는 평문 원본과 서명키를 두지 않고, 클라이언트가 애플리케이션 서명을 검증하며, NAS 방화벽은 WireGuard를 통해 API 포트만 허용한다.
- 잔여 위험: VPS에서 TLS를 종료하는 MVP 구조에서는 VPS 관리자 권한을 얻은 공격자가 초대 코드와 API 본문을 볼 수 있다.
- 향후 선택: 이 위험까지 줄여야 하면 NAS TLS 종료와 VPS TCP 중계 방식으로 전환한다.

### 원본 IP 노출

- 위험: DNS 기록, IPv6, 리디렉션, 오류 메시지, 설치기 문자열, 과거 DDNS 기록을 통해 NAS 주소가 노출될 수 있다.
- 대응: DNS를 VPS로만 지정하고 NAS 인바운드 포트를 닫으며, 배포 전 자동·수동 유출 검사를 수행한다.
- 잔여 위험: VPS 사업자는 WireGuard 피어의 접속 IP를 확인할 수 있다.

### NAS 중단

- 위험: 신규 설치와 업데이트가 중단된다.
- 대응: 게임 실행에는 서버를 요구하지 않고, 복구 문서와 외부 백업을 유지한다.

### 오탐과 지원 부담

- 위험: OS 재설치나 하드웨어 변경이 다른 기기로 판단될 수 있다.
- 대응: 기기 키를 주 식별자로 사용하고 관리자가 초기화할 수 있게 한다.

### 라이선스 충돌

- 위험: 기존 CC 라이선스나 공동 기여자의 권리와 기기 제한이 충돌할 수 있다.
- 대응: 기술 구현 전에 배포 범위와 기여자 동의를 확정한다.

## 15. 예상 개발 순서와 규모

다음은 한 명이 작업한다는 가정의 초기 추정치다. ASAR 호환성, NAS 모델, 코드 서명 요구사항에 따라 달라질 수 있다.

- 권리·운영 정책 정리: 기술 일정과 별도로 선행
- ASAR 및 인증 프로토타입: 2~4 개발일
- 패키지 빌더: 2~4 개발일
- NAS API와 관리자 CLI: 4~7 개발일
- Windows용 Rust 설치기 MVP: 5~8 개발일
- macOS·Linux 지원: 4~8 개발일
- VPS·NAS 터널, 배포, 백업, 보안 설정: 3~5 개발일
- 파일럿과 결함 수정: 3~6 개발일

전체 플랫폼을 한 번에 구현하지 않고 Windows MVP를 먼저 완성한 뒤 macOS와 Linux를 추가한다. 1단계 프로토타입에서 Rust ASAR 처리 방식이 확정되기 전에는 전체 일정을 고정하지 않는다.

## 16. 산출물

- Rust 공용 설치기 소스와 플랫폼별 빌드 설정
- NAS 활성화 API 소스와 컨테이너 설정
- VPS 리버스 프록시·WireGuard 배포 설정
- NAS WireGuard 피어 및 방화벽 설정
- 관리자 CLI
- 데이터베이스 마이그레이션
- 개인화 패키지 빌더
- 패키지 및 라이선스 형식 명세
- 외부 API 명세
- 사용자 설치 안내서
- 관리자 승인·철회·기기 변경 운영서
- 백업·복원 및 서명키 사고 대응서
- 개인정보 및 배포 지문 고지문
- 교차 플랫폼 시험 결과

## 17. 구현 전 확인할 정보

- NAS 제조사와 정확한 모델명
- NAS CPU 아키텍처와 메모리
- 컨테이너 또는 가상 머신 지원 여부
- VPS 운영체제, CPU, 메모리, 월간 전송량, 방화벽 정책
- VPS 제공사의 UDP 및 WireGuard 사용 가능 여부
- 도메인 보유 여부와 DNS 관리 방식
- 가정 회선의 IPv4 NAT·CGNAT 여부와 IPv6 사용 여부
- 예상 수령인 수
- 우선 지원할 운영체제
- 인터넷이 없는 환경에서 최초 설치를 허용해야 하는지 여부
- 기기 변경을 몇 회까지 허용할지
- 현재 공개본과 향후 비공개본의 라이선스 처리 방침
- 각 번역·이미지·영상 기여자의 재배포 제한 동의 여부

이 정보를 확정한 뒤 1단계 프로토타입의 기술 사양과 작업 티켓을 작성한다.
